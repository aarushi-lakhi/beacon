# OpenAI API Key - required for live agent runs
# Leave blank or omit to run in demo mode with pre-computed results
OPENAI_API_KEY=sk-your-key-here

# Optional: set to "demo" to always use pre-computed results
BEACON_MODE=demo
